
  # Build website version

  This is a code bundle for Build website version. The original project is available at https://www.figma.com/design/AXFcylSjGS1OEUABcAQJIS/Build-website-version.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  